# Working context for Claude sessions: hey-donut

A custom wake word ("hey donut") and its training pipeline. Start with `README.md`.
The training workspace (samples, features, models) is generated and never committed.
Maintainers keep their session-specific guidance in `CLAUDE.local.md`, which is gitignored.
